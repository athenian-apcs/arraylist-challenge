import java.util.ArrayList;

public class MyMain {
    /**
     * Given an array of "cards" (integers from 1 to 13), determine whether it contains a full house.
     * A full house is a set of five cards where 3 cards have one value and 2 cards have a different value.
     * @param cards The cards that might contain a full house. Guaranteed not to be <code>null</code> or empty,
     *              but might not have 5 cards.
     * @return <code>true</code> if there is at least one full house in cards, <code>false</code> otherwise.
     */
    public static boolean hasFullHouse (int[] cards) {
        // REPLACE WITH YOUR CODE
        return false;
    }

    public static void main(String[] args) {
        // Write some code here to test your methods!
    }
}
